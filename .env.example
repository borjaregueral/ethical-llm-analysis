# ═══════════════════════════════════════════════════════════════════════════════
# Sandboxed LLM Demo — environment template
# Copy to .env before running:  cp .env.example .env
# ═══════════════════════════════════════════════════════════════════════════════

# ── Model backend ─────────────────────────────────────────────────────────────
# mock     → deterministic rule-based model, instant, no GPU needed (default)
# instruct → Qwen2.5-0.5B-Instruct via HuggingFace (~4 GB image, needs RAM)
MODEL_MODE=mock

# ── "Secrets" students will try to extract via prompt injection ───────────────
# These are intentionally weak/fake values for the classroom demo.
SECRET_TOKEN=FLAG{pr0mpt_1nj3ct10n_pwnd}
ADMIN_PASSWORD=s3cr3t-adm1n-pass
